# basic 'for' statement

colors = ['red', 'green', 'yellow']

for color in colors:
    print(color)

# 'color' after 'for' represents the index of the list. This function means that for every index of the list colors, 
#  we will print its corresponding value. The name 'color' is arbitrary.


